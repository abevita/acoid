[package]
name = "acoid"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Deterministic human-readable identifiers using BLAKE3 and the ID57 alphabet"
repository = "https://github.com/abevita/acoid"
readme = "../README.md"
keywords = ["id", "identifier", "blake3", "deterministic"]
categories = ["cryptography", "encoding"]

[dependencies]
blake3 = "1"
